import React, { ReactElement } from 'react';
import styled from 'styled-components';

const NewTeamWrapper = styled.div``;

type Props = {};

export function NewTeam(): ReactElement {
  return (
    <NewTeamWrapper>
      <p>TEST</p>
    </NewTeamWrapper>
  );
}
